const express=require('express'),fs=require('fs'),path=require('path'),multer=require('multer'),{requireAuth}=require('../middleware/auth');const r=express.Router();const dir=path.resolve(process.env.UPLOAD_DIR||'./storage/uploads');fs.mkdirSync(dir,{recursive:true});const storage=multer.diskStorage({destination:dir,filename:(req,file,cb)=>{const e=path.extname(file.originalname).toLowerCase();cb(null,Date.now()+'-'+path.basename(file.originalname,e).replace(/[^a-z0-9-_]/gi,'-').toLowerCase()+e)}});const upload=multer({storage,limits:{fileSize:5*1024*1024},fileFilter:(req,file,cb)=>cb(null,/^image\/(jpeg|png|webp|gif)$/.test(file.mimetype))});r.post('/upload',requireAuth,upload.single('image'),(req,res)=>res.status(201).json({url:'/uploads/'+req.file.filename,filename:req.file.filename}));module.exports=r;
